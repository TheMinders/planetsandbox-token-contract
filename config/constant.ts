import { BigNumber } from "@ethersproject/bignumber";

export const DECIMAL_MULTIPLIER = BigNumber.from(10).pow(18);
export const ONE_MILLION = BigNumber.from(10).mul(6);
export const DAYS_OF_MONTH = 30; // 30 days as 1 month
